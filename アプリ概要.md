# PitaMado アプリ概要

PitaMado は、macOS のウインドウ配置を素早く整えるための個人用メニューバーアプリです。

Dock には表示せず、メニューバーのアイコンから操作します。macOS のアクセシビリティ API を使って、通常のアプリウインドウを移動・リサイズします。

## できること

- 前面ウインドウを左半分、右半分、中央、最大化に配置する
- 表示中の通常ウインドウをまとめて整列する
- ターミナル系アプリを下段、それ以外を上段に分けて整列する
- Chrome、Finder、Terminal の定番配置を作る
- 定番配置は、現在表示中の Chrome、Finder、Terminal のウインドウだけを配置する

## お気に入り配置

`お気に入り配置` は、普段の作業デスクトップを素早く作るための配置です。

画面全体を左 1/3 と右 2/3 に分けます。

- 左 1/3: Chrome 系ブラウザを縦いっぱいに配置
- 右 2/3 上段: Finder を2つ横並びで配置
- 右 2/3 下段: Terminal 系アプリを2つ横並びで配置

右上の Finder は、左を画面幅の 20%、右を画面幅の 46%（右側 2/3 内では 30:70）にし、上半分の高さに配置します。

右下の Terminal は 50:50 の比率で均等に配置します。

現在のデスクトップに Chrome、Finder、Terminal のウインドウが足りない場合でも、新規ウインドウは自動で開きません。表示中の対象ウインドウだけを配置します。

Chrome 系として扱うアプリ:

- Google Chrome
- Chrome Beta / Dev / Canary
- Microsoft Edge
- Brave
- Vivaldi
- Arc

Terminal 系として扱うアプリ:

- Terminal
- iTerm2
- Warp
- Ghostty
- Hyper
- WezTerm

## デスクトップ / Spaces 対応

PitaMado は、基本的に現在のデスクトップに見えているウインドウを対象にします。

複数デスクトップを使っている場合に、別デスクトップの既存ウインドウを勝手に掴まないよう、表示中ウインドウとの一致確認を優先しています。

## メニューバーアイコン

メニューバーアイコンは、アプリ内の次のファイルとして管理しています。

```text
PitaMado/Resources/PitaMadoMenuIcon.svg
```

左右に並んだ2つのウインドウを表すアイコンです。

## 必要な権限

PitaMado がウインドウを操作するには、macOS のアクセシビリティ権限が必要です。

```text
システム設定 > プライバシーとセキュリティ > アクセシビリティ > PitaMado
```

この権限が無い場合、アプリは起動していてもウインドウを移動できません。

## コード署名

ローカルビルドでは、次の固定コード署名証明書を使います。

```text
PitaMado Local Code Signing
```

この固定署名により、再ビルド後も macOS のアクセシビリティ許可が維持されやすくなります。証明書が見つからない場合、`build-local.sh` は ad-hoc 署名にフォールバックします。

ビルドコマンド:

```sh
./build-local.sh
```

固定署名で成功した場合、出力に次の行が出ます。

```text
Signed with PitaMado Local Code Signing
```

## 主なファイル

- `PitaMado/AppDelegate.swift`: 起動処理、確認用引数
- `PitaMado/MenuBarController.swift`: メニューバーアイコン、メニュー操作
- `PitaMado/WindowManager.swift`: ウインドウ取得、移動、整列、お気に入り配置
- `PitaMado/PermissionManager.swift`: アクセシビリティ権限チェック、設定画面を開く処理
- `build-local.sh`: ローカルビルド、リソースコピー、コード署名

## 現在のビルド先

```text
build/PitaMado.app
```
